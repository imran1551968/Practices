#include <stdio.h>

int main() {
    int i = 1;  // Initialize i to a value less than 10

    while (i <= 100) {  // Change the condition to check if i is less than or equal to 10
        printf("Allah is the greatest\n");
        scanf("%d", &i);
        i = i+1;
    }

    return 0;
}

